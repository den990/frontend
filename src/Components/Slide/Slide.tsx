import React from "react";
import style from './Slide.module.css';

const Slide = ( Props: { blockList: Block[], selectedBlockList: Block[], background: color | pictureBackground, slideIndex: number }) => {
    return;
}

export default Slide;